# ⚡ بدء سريع

## 🚀 تشغيل التطبيق:

### 1. شغل Fly.io Proxy (في terminal منفصل):
```bash
flyctl proxy 5432 -a emingo-db
```

### 2. شغل التطبيق:
```bash
npm run dev:all
```

هذا سيشغل:
- ✅ API Server على `http://localhost:3001`
- ✅ Vite Dev Server على `http://localhost:8080`

---

## 📝 ملاحظات:

- **API Server** يتعامل مع قاعدة البيانات (يستخدم `pg`)
- **Frontend** يتصل بالـ API Server عبر `fetch` API
- **Vite Proxy** يوجه `/api/*` إلى `http://localhost:3001/api/*`

---

## ✅ التحقق:

افتح المتصفح على: `http://localhost:8080`

يجب أن يعمل التطبيق بدون أخطاء! 🎉

